using RDE.Structs;
using RDE.Structs.Enums;

namespace RDE.Components;

public static class FieldFiller {

    public static (Error, Vector2) Fill(string[,] input)
    {
        
        return new (Error.E000, new());
    }
}